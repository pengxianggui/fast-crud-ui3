# 导出

导出功能入口在“更多 > 导出”:
![img_14](../../img/img_14.png)

点击导出，进入导出面板：
![img_15](../../img/img_15.png)

你可以定制导出excel的表头，以及设置某些列不导出，最后根据需要导出当前页或全部数据。

> 由于excel单个单元格难以支持多图/多文件，因此`FastTableColumnImg`和`FastTableColumnFile`配置为multiple时, 导出的内容不渲染图片，
> 为原始json数据。
